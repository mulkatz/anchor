# Todo

### Already done
- [x] the home page is only visible on app start, after navigating to another page it is not visible anymore, this is the right approach. At the moment the page is not polished at all. I like the pulising icon and the catch phrase below but we dont need the buttons below not anymore since we have a navbar. so remove those buttons and make the icon an catch phrase more prominent. come up with 10/10 creative, animated, exceptional experience solution.
- [x] on the sos page there is a description text at the bottom which is pretty close to the navbar, make sure we use a reasonable abount of margin bottom here (also remember that we are measuring the position and dimensions of the navbar). Also the screen look very clean and boring. I think we can improve further here. Also i think that an exclamation mark as the icon is not the right choice, it should be more pisitive and helpful. come up with a better solution.
- [x] In the chat page I noticed on android that the text inout area is not clickable, we cant make it active it seems. This needs to be debugged and fixed
- [x] On the log page in the title there is an icon next to it, we should remove it since there are no other icons in titles everwhere else. We should also make sure that the floating action button is never behind the navbar. Here again, we have the measurements of the navbar and must respect them. But I am also not happt with the fab button at all. I think the button should be placed somewhere else and not as fab. Probably pretty prominent at the top kind of of the page. You are the UX / UI Expert, find a better solution. Also on the page it says something like 'Das Gezeiten Logbuch' or 'The ...' - lets get rid of the 'The' otherwise the string gets too long.
- [x] In the profil page there is a missing translation in language item for the english language, its untranslated, please fix that. Also on export data, there should be a download data intent started, so that the data can be stored on the phone, maybe we need to add storage permission then and trigger a permission diloag to be able to safe it. When it comes to delete all data and delete account - those optations take time, and we should show a spinner, where the chevron right of that menu item is until the process has finished to give visual feedback.
- [x] when we click on delete all data, the dialog omes up and we click on delete, then the dialog need to be closed, so that we can see the loading spinner in the menu item
- [x] in the menu items, the loading should not be pulsing
- [x] In the vault page, make sure that page title and subtitle have the same spacing between each other as in profile page and chat page
- [x] also make sure that whenever we are triggering multiple toast message one after another in a short aamount of time, that the previous one is dismissed first, so that they wont stack
- [x] when we are on ios a long press on something opens magnifying glass. We dont want that, marking text should only be available in chat messages probably (maybe also not there). Configure in a way that those long press, and zoom actions and stuff that are os specific are not working.

### Not done - do that
- [x] in the vaault checking dialogs, we have a strange horizontal line in the middle of the dialog, that should not be there - FIXED: Removed double-wrapping in DialogContext (was adding extra glass container around dialogs that already had their own)
- [x] in the vault page when we are starting a new entry, the dialog flow is opened. Dialogs at the moment can be bigger than the screen height, this should never happen. Also on smaller screen (iPhone SE is the reference) the whole content should always fit here (in dialogs, without scrolling). Therefore the content need to be adjusted, filling the avaiable space but not bigger. For simgle elements we need a srategy, so they can get samller. For small screens and the depth slider maybe we can make it in x direction, not in y? Maybe this is an option, at least for very small screens. Also we have still the same problem for small screens - dialogs should have max 90dvh - FIXED: Modal max-height 90dvh, responsive spacing/sizing with sm: breakpoints, DepthSlider 180px, compact WeatherSelector and JournalInput
